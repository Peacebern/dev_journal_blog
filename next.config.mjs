/** @type {import('next').NextConfig} */

// next.config.js

const nextConfig = {
  reactStrictMode: true, // Keeping your existing configuration

  // You don't need experimental.allowedDevOrigins here
};

export default nextConfig;

// const nextConfig = {
//   reactStrictMode: true,
// };

// export default nextConfig;

// next.config.js

// next.config.js

// const nextConfig = {
//   reactStrictMode: true,  // Keeping your existing configuration

//   experimental: {
//     allowedDevOrigins: ['http://localhost:3000', 'http://192.168.137.1:3000'], // Allow cross-origin requests
//   },
// };

// export default nextConfig;
